apiVersion: v1
kind: Service
metadata:
  name: hello-app-worker
spec:
  type: ClusterIP
  selector:
    app: hello-app-worker
  ports:
    - name: health
      port: 8081
      targetPort: 8081
      protocol: TCP
