#!/usr/bin/env sh
# seed.sh — Seed the database after deploy.
# In a real project this would insert test/default data.
# For the tutorial, we simply log the action.

set -e

echo "🌱 Seeding database..."
echo "   (no-op for tutorial — add real seed logic here)"
echo "✅ Seed complete."
